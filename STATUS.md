# STATUS — الحالة المتحركة

للثابت (بنية · صلاحيات · قواعد · تشخيص) شوف `CLAUDE.md`.
هذا الملف بينحدّث بآخر كل جلسة. لا تكتب فيه أي شي مستنتج من أمر — اكتب الأمر.

---

## الحالة الآن

| البند | الحالة |
|---|---|
| قاعدة | متزامنة — تحقّق: `npx supabase migration list` (المحلي لازم يطابق البعيد بالحرف) |
| ويب | منشور على **`sakanna.ps`** (Custom Domain رئيسي) + `www.sakanna.ps` (تحويل ٣٠١ للرئيسي) + `sakan.abdallatif-tiyah.workers.dev` (لسا شغّال بالتوازي، `workers_dev=true` صراحة). هوية بصرية جديدة (أزرق/كهرماني) + عربي/إنجليزي كامل على `index.html` · `page.html` · `institutions.html` · `account.html`. **`owner.html` عربي بس عمداً** (رابط موقّع خاص، بدون مبدّل لغة — قرار مقفول، شوف `CLAUDE.md`). مركز التحكم عربي بس، وتوكناته القديمة لسا زي ما كانت عمداً (قرار متحفّظ، شوف `CLAUDE.md`). النشر يدوي: `npx wrangler deploy` |
| حسابات | مفعّلة وإضافية — `public/account.html`. مالك/باحث يقدر يفعّل صفة أو الاثنتين فوق حساب واحد. إشعارات تلقائية (طلب تواصل، تغيّر حالة/توثيق، قرب الانتهاء، نشر طلب، تطابق جديد) + جرس بمركز التحكم لإرسال يدوي. الفحص الأمني مرّ كامل (منهجية `set local role` بدل مستخدم Auth حقيقي — لا أداة API متاحة لإنشاء/حذف مستخدم اختبار)، بالإضافة لاختبار حي فعلي على الموقع المنشور (تسجيل → دخول فوري → تفعيل صفة → إشعار). **`Confirm email` مطفأ حالياً** (خدمة البريد المدمجة كانت ترفض التسليم لغير أعضاء الفريق أصلاً) — يعني **لا تحقّق من ملكية البريد عند التسجيل مؤقتاً**، أي حد يقدر يسجّل بأي بريد. إعادة التفعيل مرتبطة بربط SMTP مخصّص (شوف «مهام مفتوحة») |
| بيانات | لا تعتمد على أرقام محفوظة هون. استعلم مباشرة: `select count(*) from listings where status='published'` · `select status, count(*) from seeker_requests group by 1` · `select count(*) from profiles` · `select count(*) from institution_leads` · `select count(*) from owner_fees` · `select count(*) from profiles where account_uid is not null` |
| واتساب API | مجهّز بالكود، غير مفعّل — محتاج حساب Meta Business حقيقي |
| تسجيل الشركة | التسجيل بوزارة الاقتصاد الوطني (قانون التجارة الإلكترونية ٢١/٢٠٢٥) لسا ما خلص — بدونه ما في فوترة للمؤسسات |

---

## آخر جلسة

- **التاريخ:** ٢٠٢٦-٠٩-٢١ — عملة الإعلان + تفصيل الفواتير الثلاثة + تعديل كامل لبيانات الإعلان/الطلب من اللوحة
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - **عملة الإعلان:** enum جديد `currency_code` (`ILS`/`JOD`/`USD`) — عمود واحد `listings.currency` يطبّق على السعر والتأمين معاً بنفس الإعلان (قرار مقصود بعد سؤال المستخدم — عملة واحدة لا عملتان منفصلتان). نموذج «أضف سكنك» بـ`index.html` صار فيه محدّد عملة. كل مكان بيعرض السعر/التأمين بالموقع العام وبـ`owner.html`/`account.html` وبمركز التحكم صار عملة-واعي (دالة `price()`/`priceMoney()`/`priceStr()` حسب الملف) — الرسم والميزانية (`owner_fees`/`seeker_requests.budget_max`) **بقوا شيكل ثابت دايماً**، ما إلهم علاقة بعملة الإعلان (وُثّق صراحة بـ`CLAUDE.md` تحت «الدخل» منعاً لالتباس مستقبلي).
  - **الفواتير الثلاثة:** أعمدة جديدة `bills_water`/`bills_electricity`/`bills_internet` (بدل خانة "شاملة/غير شاملة" واحدة) — كل نوع فاتورة له حالة مستقلة. `bills_included` (العمود القديم) بقي، لكنه صار محسوباً تلقائياً = AND الثلاثة (تُستخدم لبادج الملخّص بكرت الإعلان بس). نموذج التسجيل وصفحة تفاصيل الإعلان بالموقع العام عرضوا الثلاثة بوضوح بدل السطر الواحد المبهم.
  - **migration واحد لكل التغيير الأول** (`20260921185049`): الأعمدة الجديدة + `submit_listing` استبدال كامل (توقيع جديد: `p_bills` الفردي انشال، صار `p_currency`+٣ معطيات فواتير، مع `drop function` صريح للتوقيع القديم لتفادي overload) + `v_listings_public`/`v_admin_listings`/`v_admin_pipeline` (أعمدة جديدة بذيل القائمة + إعادة ضبط `security_invoker` على الاثنين الأخيرين) + `my_owner_dashboard`/`owner_dashboard`/`my_seeker_dashboard` (إضافة `currency` لعرض السعر صح بلوحات المالك/الباحث الذاتية).
  - **لوحة «بانتظار المراجعة»:** جدول الطابور صار فيه عمودا «الغرف» و«أقل مدة» (كانا مفقودين تماماً). درج تفاصيل الإعلان (نفس الدرج المشترك مع «كل الإعلانات») صار يعرض: عدد الغرف، أقل مدة إيجار، الفواتير الثلاثة كل وحدة لحالها، ومواصفات السكن (`features`) — كانت **غايبة كلياً** عن اللوحة رغم إنها جزء من نموذج التسجيل (فجوة اكتُشفت أثناء المقارنة بين الفورم واللوحة).
  - **تعديل بيانات الإعلان/الطلب من اللوحة** (`20260921185205`): دالتان إداريتان جديدتان `admin_listing_update`/`admin_request_update` — تعديل كامل لكل حقل عبّاه المالك/الباحث بنموذج التسجيل العام (العنوان، الوصف، السعر، العملة، التأمين، النوع، السياسة، الفواتير الثلاثة، عدد الغرف، أقل مدة، الموقع/المدينة/المنطقة، المواصفات/الصفات، الميزانية، التدخين، تاريخ الانتقال...) بدون الرجوع للمالك/الباحث لتصحيح خطأ إدخال. **بدون `p_actor`** (قاعدة ١٦، دوال جديدة). ما بتلمس الحالة/التوثيق/الصور — هاي تبقى عبر الدوال المخصّصة الموجودة أصلاً (`admin_listing_status`/`admin_listing_verification`/`admin_listing_images`/`admin_request_status`). زر «تعديل البيانات» بدرج الإعلان وبدرج الطلب بمركز التحكم، فورم مخفي يظهر عند الضغط.
  - **صفحة تسجيل الباحث:** إزالة خيار «أفضّل عدم التحديد» من سؤال التدخين (صار سؤال إلزامي لا/نعم فقط — بدون تغيير schema، العمود `smoker` يبقى nullable للصفوف القديمة). إضافة صفة جديدة «يستقبل/تستقبل ضيوف» (`hosts_guests`) لقائمة صفات الساكن — إلى جانب الصفة المعاكسة الموجودة أصلاً «بدون استقبال ضيوف» (`no_guests`)، الاثنتان مش متبادلتان حصرياً (تاغّات حرة، مش راديو).
  - **فحص حي كامل عبر SQL مباشر** (لا واجهة — أسرع وأدق لدالتين جديدتين): `submit_listing` بعملة `USD` وفواتير مفصّلة → تحقّق القيم بالجدول → `admin_listing_update` عدّل كل الحقول (عملة، نوع، فواتير، مواصفات...) → تحقّق التعديل انطبق بالكامل. نفس الشيء لـ`submit_request`→`admin_request_update` (تضمّن تحقّق تاغ `hosts_guests` الجديد). بيانات الاختبار (الإعلان، الطلب، الصفّين بـ`profiles`، صفوف `events`/`admin_actions` المرتبطة) انحذفت بالكامل بعد التحقّق — صفر أثر على القاعدة.
  - **توثيق:** `CLAUDE.md` — enum `currency_code` جديد بقائمة الأنواع، الدالتان الجديدتان بقائمة الدوال الإدارية (٢٢→٢٤) وبقائمة رقم ١٦ (بدون `p_actor`)، فقرة توضيحية تحتهما، وتنويه صريح تحت «الدخل» إن رسم النجاح يبقى شيكل بغض النظر عن عملة الإعلان.
- **تنبيه نُشر فوراً:** تعديل `submit_listing` (حذف التوقيع القديم وإنشاء توقيع جديد) طبّق على القاعدة الحية **قبل** نشر الكود الجديد — نافذة زمنية قصيرة كان فيها نموذج «أضف سكنك» المنشور فعلياً معطّل (الواجهة القديمة بترسل `p_bills` اللي ما عاد موجود بالتوقيع الجديد). طُلب النشر الفوري بدل الانتظار، وصار `npx wrangler deploy` بنفس الجلسة — تحقّق حي بعده أكّد الكود الجديد شغّال على `sakanna.ps` (فحص نصّي لعناصر النموذج الجديدة بالصفحتين العامة والإدارية) والجذر/`/admin` سليمين. **درس للمستقبل:** أي تعديل يغيّر توقيع دالة RPC مستخدَمة من الواجهة يفترض نشر الكود فوراً بعد الميغريشن، لا الانتظار لطلب لاحق.
- **الخطوة الجاية:** لا شيء معلّق.

---

## آخر جلسة (سابقة)

- **التاريخ:** ٢٠٢٦-٠٩-٢٠ — ربط دومين `sakanna.ps` (+`www`) كـCustom Domain على الـWorker
- **آخر commit:** `0a77e32` (`23b4dc0` قبله بنفس الجلسة)
- **شو صار:**
  - أبواللطيف اشترى `sakanna.ps` من domain.ps وأشار أسماء سيرفراته لـCloudflare (`roan.ns.cloudflare.com`/`sue.ns.cloudflare.com`) بنفسه، وطلب صراحة الربط بالـWorker — **قبل** بوابة القرار الموثّقة بـ«مهام مفتوحة» (كانت مؤجّلة لما بعدها، وبدومين مختلف `sakan.ps` لا `sakanna.ps`). نُفّذ بناءً على طلب مباشر بنفس الجلسة، ما كان قرار Claude.
  - تأكّد حي أول شي (`nslookup -type=NS`) إن الـNS انتشرت فعلاً قبل أي تعديل كود.
  - `wrangler.toml`: `[[routes]]` بـ`pattern="sakanna.ps"` و`pattern="www.sakanna.ps"` (الاثنين `custom_domain=true`).
  - **فخ اكتُشف حياً:** أول `wrangler deploy` بعد إضافة الـroute الأول عطّل `workers_dev` تلقائياً (رابط `sakan.abdallatif-tiyah.workers.dev` رجع `404`) — Wrangler ما بيبقيه شغّال إلا إذا `workers_dev = true` مكتوبة صراحة. أُضيفت وأُعيد النشر، الرابطان رجعوا يشتغلوا مع بعض. مُوثّق بجدول التشخيص بـ`CLAUDE.md`.
  - `src/worker.js`: تحويل `301` من `www.sakanna.ps` للنطاق الأساسي (أول سطر بـ`fetch()`، قبل أي منطق تاني) — مانع محتوى مكرر بمسارين.
  - محاولة نشر أولى فشلت بخطأ تثبيت (`@cloudflare/workerd-windows-64` مفقود من كاش npx) — انحلّت بمسح مجلد الكاش المحدّد وإعادة المحاولة، بدون علاقة بالكود.
  - **تحقّق حي كامل بعد النشر:** `sakanna.ps`→`200` (محتوى حقيقي اتأكّد لا صفحة فاضية)، `www.sakanna.ps`→`301`→`sakanna.ps`→`200` (اتأكّد عبر `--resolve` مباشر لتجاوز كاش DNS محلي كان لسا شايل نتيجة سلبية قديمة على راوتر المستخدم)، `sakan.abdallatif-tiyah.workers.dev`→`200`، `/admin`→`307`→`/admin/` مع `X-Robots-Tag: noindex, nofollow` (نفس فحص قاعدة ٦).
  - توثيق: تحديث الملاحظة الافتتاحية بـ`CLAUDE.md` (النطاق الفعلي `sakanna.ps` لا `sakan.ps` — استثناء عن مبدأ «البنية التحتية سكن»)، قسم Cloudflare، وصف جديد بجدول التشخيص.
- **الخطوة الجاية:** لا شيء معلّق على الربط نفسه. تفعيل "Always Use HTTPS" من لوحة Cloudflare (SSL/TLS → Edge Certificates) لسا اختياري — `http://` حالياً بيرجّع `200` مباشرة بدل تحويل لـ`https` (SSL شغّال أصلاً، بس بدون فرض).

---

## آخر جلسة (سابقة)

- **التاريخ:** ٢٠٢٦-٠٩-١٥ — تعطيل طبقة «توثيق مكتبي» (desk) + تثبيت عتبات تعيين المندوب (Phases A-D)
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - **مرحلة أ (تدقيق قبل أي تعديل):** `select verification, count(*) from listings group by 1` رجع `none=2, field=1` — **صفر صفوف بـ`verification='desk'`**، فما في حاجة توقّف (الشرط المطلوب فحصه صراحة قبل أي خطوة). حصر كامل لكل إشارة لـ`desk`: زر اللوحة (`data-vf="desk"`، الوحيد القابل للضبط فعلياً)، كائنات `L_VERIF`/`VER` بالملفات الأربعة، صنف CSS `.t-desk`. تحقّق حي إضافي: `admin_listing_verification` ما فيها أي فرع مكتوب حرفياً على `'desk'` (تعميم كامل عبر `p_level::listing_verification`)، ولا أي view (`pg_views.definition ILIKE '%''desk''%'` = صفر نتائج).
  - **مرحلة ب:** حُذف سطر واحد بس من `public/admin/index.html` — زر `data-vf="desk"` بلوحة تفاصيل الإعلان. المحدّد صار ٣ خيارات فقط (`none`/`video`/`field`). `L_VERIF`/`VER` تُركت كما هي بكل الملفات (لو صف قديم بـ`desk` طلع، يترجم كنص لا enum خام). `admin_listing_verification()` بلا أي تعديل — لسا بتقبل `'desk'` لو استُدعيت مباشرة (القاعدة تبقى المرجع، وصول `service_role` الطارئ يضل شغّال). **لا تغيير schema بهالمرحلة — صفر migration.**
  - **مرحلة ج (توثيق):** فقرة جديدة تحت قاعدة ٢٤ بـ`CLAUDE.md` — `desk` قيمة تاريخية بالـenum (ما بينحذف enum value بـPostgreSQL)، اكتُشفت أثناء بناء الطبقتين **حيّة وغير موثّقة**، بدون أي نص عام يغطّيها (بالضبط الفشل اللي قاعدة ٢٤ وُجدت تمنعه). تحذير صريح: ممنوع تفعيلها من جديد بأي واجهة قبل تعريف نصّها العلني وإجراء توثيقها. `STATUS.md`: بند «تعيين المندوب» المفتوح استُبدل بعتبات دورة أولى ملموسة من أبواللطيف نفسه (٨ ملّاك وافقوا من ٢٥ اتصال، ٨ إعلانات موثّقة منشورة، ١٢ طلب تواصل، ٣ تأجيرات) — **عتبات قرار تعيين، مش بوابة القرار**؛ بوابة `settings` (`gate_rooms=100`/`gate_rentals=25`/`gate_date=2026-11-30`، تحقّق حي: بلا تغيير) تبقى معيار استمرار/توقف الاستثمار نفسه، منفصلة تماماً. لحد تحقّق العتبات، أبواللطيف بيتولى التوثيق بنفسه، فيديو أولاً.
  - **مرحلة د (نشر):** `git push` + `npx wrangler deploy` — نشر تراكمي لجلستين (طبقتا التوثيق + تعطيل desk)، `index.html`/`admin/index.html` ما كانا منشورين من الجلسة السابقة. تحقّق حي بعد النشر: الجذر `200`، `/admin` `307`→`/admin/` مع `X-Robots-Tag: noindex`.
- **تصحيح:** الجلسة السابقة افترضت عدم توفّر حساب طاقم فعلي للدخول على `/admin` (استخدمت CSS الصفحة الحقيقي مع markup معزول بدل جلسة مسجّلة كاملة للقطة الشاشة على هالأساس) — **الافتراض غلط**. حساب طاقم موجود فعلياً والدخول على `/admin` به تأكّد شغّال. بند «تجربة دخول فعلي بحساب طاقم حقيقي» انحذف من المهام المفتوحة.
- **الخطوة الجاية:** لا شيء معلّق. المتبقي من الجلسات السابقة كما هو (SMTP مخصّص، ربط بروفايل قديم، دخول OTP).

---

## آخر جلسة (سابقة)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة سادسة) — طبقتا توثيق: زيارة ميدانية + مكالمة فيديو (Phases A-F)
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - **مرحلة أ (اكتشاف):** استُعلم الـschema الحي أولاً بدل الاعتماد على `CLAUDE.md` (تحذير صريح بالطلب: أخطأ ٤ مرات قبل هيك). `listings.verification` طلع enum حقيقي (`listing_verification`: `none`/`desk`/`field`, `not null`, default `'none'`) — **مش boolean**، تحذير البوليان الثلاثي بـ`CLAUDE.md` ما بينطبق هون. حصر كامل للتابعين عبر `information_schema.view_column_usage` + مسح `routine_definition`: ٤ views بس (`v_listings_public`، `v_admin_listings`، `v_kpi_core`، `v_kpi_quality`) وكلهم إما تمرير خام أو مطابقة حرفية `= 'field'` — مناعة طبيعية ضد قيمة enum جديدة. أصغر تعديل: `alter type ... add value 'video' after 'desk'` (لا يوجد أي مقارنة `>`/`<` على الـenum بأي مكان — تحقّق حي قبل الاختيار).
  - **مرحلة ب:** `20260914211509` — القيمة `video` أُضيفت (لا يمكن حذفها لاحقاً، PostgreSQL بلا `DROP VALUE`). `listings.video_verified_at` (date، nullable) عمود جديد — الميداني عنده تاريخه أصلاً (`listing_safety.visit_date`)، الفيديو ما إله مكان بجدول فحص فيزيائي فما ينفع يشارك نفس الجدول. الميغريشن ما بيشير لـ`'video'` بأي جملة نصّية (احتياط لقيد عدم استخدام قيمة enum بنفس الترانزاكشن اللي أضافتها). فحص: صفوف موجودة (`none=2, field=1`) بلا تغيير.
  - **مرحلة ج:** `20260914211625` — `admin_listing_verification` توقيعها بلا تغيير (تقبل `p_level` نصي عام أصلاً)، أُضيف بس تسجيل `video_verified_at=current_date` لما `p_level='video'` (مقارنة نصية، بدون مرجع لقيمة enum). `has_function_privilege('anon',...)=false`. `20260914211737` — `video_verified_at` أُضيف لـ`v_admin_listings` بذيل الأعمدة (فخ الترتيب)، `security_invoker` أُعيد ضبطه (فخ التصفير)، تحقّق `reloptions` + `set local role authenticated` غير موظّف = صفر صفوف. زر رابع بلوحة الإعلان "توثيق بمكالمة فيديو". فحص حي: إعلان اختبار — `video` سجّل التاريخ، تحويله لاحقاً لـ`field` حافظ على `video_verified_at` (ما انمسح).
  - **مرحلة د:** `20260914211951` — `video_verified_at` أُضيف لـ`v_listings_public` (بلا `security_invoker` أصلاً — view عامة، لا فخ هون). `index.html`: دالة `verifBadge(x)` واحدة بدل تكرار المنطق بمكانين. النصوص الحرفية المطلوبة فقط: «موثّق بزيارة ميدانية — [التاريخ]» / «موثّق بمكالمة فيديو — [التاريخ]» (وبالإنجليزي عبر `tt()`). `desk` تُرك بشكله القديم (بدون تاريخ) — خارج نطاق النصوص المطلوبة صراحةً. **فحص حي بمتصفح فعلي** (خادم محلي، لا محاكاة): ٣ إعلانات حقيقية منشورة (ميداني عبر `admin_save_safety`، فيديو عبر `admin_listing_verification`، بدون توثيق) — لقطات كرت وتفاصيل للثلاثة أكّدت النصين الجديدين بالتاريخ الصحيح وصفر نص للإعلان غير الموثّق. بيانات الاختبار انحذفت بالكامل بعدها.
  - **مرحلة هـ:** `20260914212704` — `v_kpi_quality.video_verified_pct` عمود جديد بذيل الأعمدة بنفس منطق `field_verified_pct` بس بفلتر `video`. تحقّق حي: `field_verified_pct` بقيت `33.3` قبل وبعد (رغم وجود إعلان فيديو وقت الفحص) — إثبات عدم التضخّم، مش افتراض. نفس فحوص الفخّين (`reloptions` + غير موظّف = صف واحد بكل الأعمدة الستة `null`، نفس توقيع الحماية المعتمد بهاي الـview أحادية الصف).
  - **مرحلة و (توثيق):** `CLAUDE.md` — قاعدة ٢٤ جديدة (النصوص الحرفية + منع «موثّق» وحدها)، توضيح تحت «السياق التجاري» عن الطبقتين وسبب معنى الفيديو رغم قابليته للتحايل (مكالمة حيّة، جولة متواصلة، إظهار المدخل ورقم الباب، طلبات غير متوقّعة، تسجيل التاريخ والمندوب)، وتوضيح لقاعدة ١٤ (بند الكاميرا خاص بالزيارة الميدانية حصراً، ما ينطبق على الفيديو).
- **قرار اتخذته بنفسي دون الرجوع:** (١) `desk` (توثيق هاتفي) تُرك بلا تعديل — النصوص الحرفية المطلوبة صراحةً غطّت الميداني والفيديو فقط؛ (٢) إضافة `video_verified_at` لـ`v_admin_listings` رغم عدم ذكرها صراحة بمرحلة ج — مطابقة لمنطق `visit_date` الموجود أصلاً، يخدم نفس مرحلة "أضف محدّد اللوحة"؛ (٣) ترتيب enum بحيث `video` بين `desk` و`field` (`none < desk < video < field`) بدل آخر القائمة — تحقّقت أولاً إنه لا يوجد أي مقارنة عدم مساواة على الـenum بأي مكان، فالإدراج بالمنتصف آمن ومنطقي.
- **الخطوة الجاية:** لا شيء معلّق. الطبقتان منفّذتان بالكامل ومتحقَّق منهما حياً — النشر الفعلي (`wrangler deploy`) لسا ما صار (الكود بالريبو، محلي فقط زي كل جلسة سابقة إلى أن يُطلب صراحة).

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة خامسة) — إصلاح `owner_repeat_pct` بعد أثر إصلاح `submit_listing` الجانبي
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - الجلسة السابقة (إصلاح ثغرة انتحال `submit_listing`، تحت) وثّقت أثراً جانبياً مقبولاً بس غير مفحوص: `owner_repeat_pct` (`v_kpi_quality`) بيتجمّع بـ`listings.owner_id`، وصار كل مالك متكرر بدون حساب ياخد `owner_id` جديد كل مرة — يعني المؤشر هينخفض عن الواقع دايماً تقريباً. فحص حي أكّد الضرر الفعلي قبل أي تعديل: `select owner_repeat_pct from v_kpi_quality` رجع `0.0`.
  - قُرئ تعريف الـview الحي (`pg_get_viewdef`) قبل أي تعديل — خمسة أعمدة (`contact_to_rent_pct`/`median_days_to_rent`/`owner_repeat_pct`/`field_verified_pct`/`report_rate_pct`)، والاثنان فقط المتعلقان بالتعديل يلزم الحفاظ عليهما بنفس الاسم/الموضع (فخ `create or replace view` بجدول التشخيص).
  - **التعديل:** تعبير `owner_repeat_pct` صار يجمّع بـ`profiles.phone` (عبر `join profiles p on p.id = listings.owner_id`) بدل `listings.owner_id` مباشرة — بقية الأعمدة الأربعة بلا أي تغيير، بنفس الترتيب بالضبط.
  - **الفخّان الموثّقان بـ`CLAUDE.md` طُبّقا حرفياً:** (أ) تحقّق `information_schema.columns` بعد الاستبدال أكّد الأعمدة الخمسة بنفس الاسم والترتيب (٣ = `owner_repeat_pct` زي ما كانت)؛ (ب) `create or replace view` صفّر `security_invoker` كما متوقّع — `alter view ... set (security_invoker = on)` بنفس الـmigration، وتحقّق `select reloptions from pg_class where relname='v_kpi_quality'` رجع `{security_invoker=on}`.
  - **فحص الحماية:** `set local role authenticated` بـ`jwt.claims` لمستخدم وهمي غير موظّف (`is_staff()` بيرجّع false) رجّع صفاً واحداً **بكل القيم `null`** — مش "صفر صفوف" حرفياً (هاي view بترجع صف واحد ثابت من subqueries عددية، مش جدول صفوف)، لكنها نفس الإشارة: كل الأعمدة الخمسة (حتى اللي ما بتلمس `profiles` أصلاً زي `field_verified_pct`) رجعت `null` لنفس السبب — سياسات `staff_read` على `listings`/`contact_requests`/`reports`/`profiles` (الأربعة، تحقّق حي بـ`pg_policies`) بتحجب كل صف عن غير الموظف، فكل الـsubqueries الفرعية بترجع صفر صف وتتحوّل لـ`null` عبر `nullif(...,0)`. **التحقّق من الجدول تحت الـview كافٍ هون، مش مطلوب فحص إضافي خاص بـ`profiles`** لأنها محكومة بنفس النمط بالضبط.
  - **إثبات حي إن التجميع صحيح:** إنشاء إعلانين وهميين بنفس رقم الهاتف (`0599000222`) باسمين مختلفين عبر `submit_listing` (بعد إصلاحها الأخير، كل استدعاء ياخد `owner_id` مختلف فعلاً) — `owner_repeat_pct` قفز من `0.0` لـ`25.0`، يعني التكرار انضبط بالهاتف رغم اختلاف `owner_id`. البيانات الوهمية (٢ إعلان، ٢ بروفايل، صفوف `events`) انحذفت بعدها، والمؤشر رجع لقيمته الحقيقية `0.0` (لا تكرار فعلي حالياً بالبيانات — النتيجة نفسها قبل وبعد التعديل، بس الآن صحيحة منطقياً لا صدفة).
  - توثيق: تحديث سطر «الحسابات» بـ`CLAUDE.md` (كان بيوصف الأثر كـ"بيقلّل تعداد" بصيغة المستقبل، صار يشير للإصلاح الفعلي)، وإضافة شرح مفصّل تحت «السياق التجاري» بجانب تعريف المؤشر نفسه — يوضّح سبب التجميع بالهاتف، ولماذا آمن هون تحديداً رغم منعه بالكتابة بمكان تاني، مع تحذير صريح **ممنوع التحويل رجوعاً لـ`owner_id`**.
- **الخطوة الجاية:** لا شيء معلّق على هالإصلاح.

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة رابعة) — إصلاح ثغرة انتحال بـ`submit_listing`: إلغاء الربط التلقائي بمطابقة الهاتف
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - قُرئ تعريف `submit_listing` الحي أولاً (`pg_get_functiondef`) بدل الاعتماد على الذاكرة أو `CLAUDE.md`: كانت تعمل `select id from profiles where phone = trim(p_phone) and role='owner' limit 1` وتعيد استخدام أي صف موجود كـ`owner_id` للإعلان الجديد — نفس ثغرة الانتحال اللي انسدّت سابقاً بـ`link_account_role()` (الهاتف مش مُتحقَّق منه بأي OTP، فأي حدا يعرف رقم مالك حقيقي يقدر ينشر إعلاناً منسوباً لبروفايله).
  - **تقييم الخيارين المطروحين قبل الاختيار:** (أ) إلغاء إعادة الاستخدام نهائياً — صف `profiles` جديد بكل استدعاء؛ (ب) إبقاء إعادة الاستخدام بس لما الإعلان يُنشأ عبر رابط مالك موقّع أو جلسة `authenticated`. تحقّق عملي حسم الاختيار: `submit_listing` بتُستدعى حصراً من `index.html` (`grep` على كل `public/`، استدعاء وحيد)، ودايماً بمفتاح `anon` الثابت بالهيدر (`Authorization: Bearer <anon key>`) — ما في أي مسار حالي يمرّر توكن جلسة مستخدم حقيقي، و`owner.html` (الرابط الموقّع) أصلاً ما فيها نموذج إنشاء إعلان جديد، بس عرض/إدارة الموجود. يعني الخيار (ب) كان بده يتطلّب بنية تحتية جديدة بالكامل (تمرير JWT من `index.html`، أو نموذج إنشاء إعلان جديد بـ`owner.html`) — بالضبط النوع اللي الطلب منع اختراعه صراحة. اخترت **الخيار (أ)**.
  - **التنفيذ** (migration `20260914204457_submit_listing_no_phone_owner_reuse`): `create or replace function submit_listing(...)` — حُذف الـ`select`/`if v_owner is null` بالكامل، صار `insert into profiles (...) returning id into v_owner` ينفّذ دايماً بلا شرط. التوقيع (أسماء/أنواع/ترتيب المعطيات) بدون أي تغيير، فما احتجت `drop function` ولا إعادة `grant` (فخ التوقيعات المتعددة بجدول التشخيص ما ينطبق هون).
  - **فحص حي:** استدعاءان متتاليان بنفس رقم الهاتف (`0599000111`) باسمين مختلفين ("مالك اختبار أ" ثم "منتحل") أنتجا `owner_id` مختلف تماماً لكل واحد (تأكّد بـ`select ... join profiles`). البيانات الوهمية (٢ إعلان، ٢ بروفايل، صفوف `events` المرافقة) انحذفت بعدها وتأكّد الحذف. `has_function_privilege('anon'/'authenticated', ..., 'execute')` = `true` للاثنين بعد التعديل — مسار بدون تسجيل دخول لسا شغّال بدون أي قيد إضافي.
  - **الأثر المقبول:** ملّاك بيكرروا التسجيل بدون حساب هيحصلوا على صف `profiles` جديد (`owner_id` مختلف) كل مرة بدل إعادة استخدام صفهم القديم. انعكاسان بمركز التحكم: (أ) `v_admin_owners` هيعرض عدة صفوف لنفس الشخص الفعلي (نفس الهاتف ظاهر بعمود `phone` بكل صف، فالطاقم يقدر يميّزها بصرياً)، وكل صف بياخد `listings_total`/`fees_due`/إلخ خاصة فيه بس، مش مجمّعة؛ (ب) `v_kpi_quality.owner_repeat_pct` (مؤشر PMF الأساسي بالمشروع) بيحسب التكرار بتجميع `listings.owner_id` — يعني ملّاك متكررين بدون حساب هيبانوا "أول مرة" كل مرة، فالمؤشر هينخفض عن الواقع لحد ما يفعّلوا حساب (`account_uid`) أو الطاقم يدمج يدوياً. **لا يوجد أداة دمج تلقائي لصفوف مكررة** — `admin_link_profile()` بتربط صف واحد بحساب Auth موجود، ما بتنقل إعلانات صف لصف تاني.
  - توثيق: أُضيف سطر بقسم «الحسابات» بـ`CLAUDE.md` يوصل مبدأ `link_account_role` بـ`submit_listing` (نفس المبدأ، نفس السبب)، ويوثّق الأثر على `owner_repeat_pct` وغياب أداة الدمج.
- **قرار اتخذته بنفسي دون الرجوع:** لم أبنِ أداة دمج/تجميع صفوف مكررة رغم إنها فجوة واضحة نتجت عن الإصلاح — خارج نطاق الطلب الصريح (إصلاح الثغرة فقط)، وتحتاج قرار تصميم منفصل (دمج تلقائي بالهاتف بيرجّع نفس مخاطر الانتحال، ودمج يدوي يحتاج واجهة إدارية جديدة).
- **الخطوة الجاية:** لا شيء معلّق على هالإصلاح. لو تكرار البروفايلات المكررة صار ملحوظاً بلوحة `v_admin_owners`، القرار المؤجّل هو بناء أداة دمج يدوي صريحة (`admin_merge_owner_profiles()` أو ما شابه) — لسا ما انطلب.

---

## مهام مفتوحة — بترتيب الأولوية

- [ ] **مزوّد بريد مخصّص + إعادة تفعيل تأكيد البريد** — مرتبط بربط النطاق. الحل المؤقت الحالي (`Confirm email` مطفأ) يفتح التسجيل الفوري بدون بريد إطلاقاً، وهذا مقصود مؤقتاً فقط — أي مستخدم يُنشئ حساباً بأي بريد حتى لو غير مملوك له فعلياً، لأنه ما في تحقّق. عالج قبل أي إعلان علني عن الحسابات
- [ ] قرار مؤجّل: **ربط البروفايل القديم بالتوكن الموقّع** — حالياً المالك صاحب الإعلانات ينشئ حساباً فيحصل على بروفايل فارغ (`link_account_role` ما بتربط بمطابقة هاتف عمداً)، والربط يدوي بـ`admin_link_profile`. يُبنى حين يتكرر الربط اليدوي أكثر من مرة أسبوعياً
- [ ] قرار مؤجّل: **دخول برمز لمرة واحدة (OTP)** بجانب كلمة السر لا بدلاً منها — قابل للتراجع، عائد لصاحب المشروع
- [ ] حذف حسابات Auth التجريبية (`abdallatif.tiyah+audit-*`/`+livetest-*@gmail.com`) يدوياً من `/auth/users` إذا حبيت قائمة نظيفة — ما انحذفت برمجياً (لا أداة Auth Admin API متاحة بالجلسة)
- [ ] فجوة العرض ببيرزيت — مناطق معرّفة بالقاعدة، المخزون معدوم، والطلب غير مُتحقَّق منه ميدانياً بعد — كل بيانات الموقع تجريبية
- [ ] قرار مؤجّل عمداً: **تعيين المندوب** — مربوط بعتبات التحقّق من السوق، أرقام أبواللطيف الخاصة بدورة أولى (**مش بوابة القرار** — أقل عمداً من أرقام `settings.gate_rooms`/`gate_rentals`/`gate_date` الحيّة وغير المعدَّلة: ١٠٠ غرفة موثّقة و٢٥ تأجير مؤكد قبل ٢٠٢٦-١١-٣٠، هاي تبقى بوابة استمرار/توقف الاستثمار نفسه):
  - ٨ ملّاك وافقوا على التوثيق من ٢٥ اتصال
  - ٨ إعلانات موثّقة منشورة (فيديو أو زيارة)
  - ١٢ طلب تواصل عليها
  - ٣ تأجيرات مؤكدة

  تحقّق هالأرقام = قرار تعيين مندوب. بوابة `settings` تضل معيار الاستمرار/التوقف بالاستثمار، منفصلة تماماً. لحد تحقّق عتبات التوثيق، أبواللطيف بيتولى التوثيق بنفسه، فيديو أولاً.
- [ ] تعبئة `name_en` يدوياً للمدن/المناطق الجديدة لما تنضاف (الموجودة معبّاة)
- [ ] استكمال التسجيل بوزارة الاقتصاد الوطني — شرط لفوترة المؤسسات
- [ ] قرار مستقبلي: إعادة تصميم بصرية كاملة لمركز التحكم (بقيت بتوكناتها القديمة عمداً هالجلسة)
- [ ] اختياري: تفعيل "Always Use HTTPS" من Cloudflare (SSL/TLS → Edge Certificates) — `http://sakanna.ps` حالياً بيرجّع `200` مباشرة بدل تحويل لـ`https`

---

## سجل الجلسات (آخر ٥ — السادسة بتنحذف، ما في أرشفة)

| تاريخ | الجلسة | commit |
|---|---|---|
| ٢٠٢٦-٠٩-٢٠ | ربط دومين `sakanna.ps` (+`www`) كـCustom Domain على الـWorker، إبقاء `workers.dev` شغّال بالتوازي | `23b4dc0`/`0a77e32` |
| ٢٠٢٦-٠٩-١٥ | تعطيل توثيق مكتبي (desk) من اللوحة + تثبيت عتبات تعيين المندوب | (بلا commit جديد) |
| ٢٠٢٦-٠٩-١٤ | طبقتا توثيق: زيارة ميدانية + مكالمة فيديو (`listing_verification` enum، `video_verified_at`، `v_kpi_quality.video_verified_pct`، نصوص علنية حرفية) | `af9e692` |
| ٢٠٢٦-٠٩-١٤ | إصلاح `owner_repeat_pct` (`v_kpi_quality`) — تجميع بـ`profiles.phone` بدل `owner_id` بعد أثر إصلاح `submit_listing` الجانبي | `510c3b9` |
| ٢٠٢٦-٠٩-١٤ | إصلاح ثغرة انتحال بـ`submit_listing` — إلغاء الربط التلقائي بمطابقة الهاتف (نفس مبدأ `link_account_role`) | `62ee504` |
