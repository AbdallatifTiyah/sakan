# STATUS — الحالة المتحركة

للثابت (بنية · صلاحيات · قواعد · تشخيص) شوف `CLAUDE.md`.
هذا الملف بينحدّث بآخر كل جلسة. لا تكتب فيه أي شي مستنتج من أمر — اكتب الأمر.

---

## الحالة الآن

| البند | الحالة |
|---|---|
| قاعدة | متزامنة — تحقّق: `npx supabase migration list` (المحلي لازم يطابق البعيد بالحرف) |
| ويب | منشور على `sakan.abdallatif-tiyah.workers.dev`. هوية بصرية جديدة (أزرق/كهرماني) + عربي/إنجليزي كامل على `index.html` · `page.html` · `institutions.html` · `account.html`. **`owner.html` عربي بس عمداً** (رابط موقّع خاص، بدون مبدّل لغة — قرار مقفول، شوف `CLAUDE.md`). مركز التحكم عربي بس، وتوكناته القديمة لسا زي ما كانت عمداً (قرار متحفّظ، شوف `CLAUDE.md`). النشر يدوي: `npx wrangler deploy` |
| حسابات | مفعّلة وإضافية — `public/account.html`. مالك/باحث يقدر يفعّل صفة أو الاثنتين فوق حساب واحد. إشعارات تلقائية (طلب تواصل، تغيّر حالة/توثيق، قرب الانتهاء، نشر طلب، تطابق جديد) + جرس بمركز التحكم لإرسال يدوي. الفحص الأمني مرّ كامل (منهجية `set local role` بدل مستخدم Auth حقيقي — لا أداة API متاحة لإنشاء/حذف مستخدم اختبار)، بالإضافة لاختبار حي فعلي على الموقع المنشور (تسجيل → دخول فوري → تفعيل صفة → إشعار). **`Confirm email` مطفأ حالياً** (خدمة البريد المدمجة كانت ترفض التسليم لغير أعضاء الفريق أصلاً) — يعني **لا تحقّق من ملكية البريد عند التسجيل مؤقتاً**، أي حد يقدر يسجّل بأي بريد. إعادة التفعيل مرتبطة بربط SMTP مخصّص (شوف «مهام مفتوحة») |
| بيانات | لا تعتمد على أرقام محفوظة هون. استعلم مباشرة: `select count(*) from listings where status='published'` · `select status, count(*) from seeker_requests group by 1` · `select count(*) from profiles` · `select count(*) from institution_leads` · `select count(*) from owner_fees` · `select count(*) from profiles where account_uid is not null` |
| واتساب API | مجهّز بالكود، غير مفعّل — محتاج حساب Meta Business حقيقي |
| تسجيل الشركة | التسجيل بوزارة الاقتصاد الوطني (قانون التجارة الإلكترونية ٢١/٢٠٢٥) لسا ما خلص — بدونه ما في فوترة للمؤسسات |

---

## آخر جلسة

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة خامسة) — إصلاح `owner_repeat_pct` بعد أثر إصلاح `submit_listing` الجانبي
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - الجلسة السابقة (إصلاح ثغرة انتحال `submit_listing`، تحت) وثّقت أثراً جانبياً مقبولاً بس غير مفحوص: `owner_repeat_pct` (`v_kpi_quality`) بيتجمّع بـ`listings.owner_id`، وصار كل مالك متكرر بدون حساب ياخد `owner_id` جديد كل مرة — يعني المؤشر هينخفض عن الواقع دايماً تقريباً. فحص حي أكّد الضرر الفعلي قبل أي تعديل: `select owner_repeat_pct from v_kpi_quality` رجع `0.0`.
  - قُرئ تعريف الـview الحي (`pg_get_viewdef`) قبل أي تعديل — خمسة أعمدة (`contact_to_rent_pct`/`median_days_to_rent`/`owner_repeat_pct`/`field_verified_pct`/`report_rate_pct`)، والاثنان فقط المتعلقان بالتعديل يلزم الحفاظ عليهما بنفس الاسم/الموضع (فخ `create or replace view` بجدول التشخيص).
  - **التعديل:** تعبير `owner_repeat_pct` صار يجمّع بـ`profiles.phone` (عبر `join profiles p on p.id = listings.owner_id`) بدل `listings.owner_id` مباشرة — بقية الأعمدة الأربعة بلا أي تغيير، بنفس الترتيب بالضبط.
  - **الفخّان الموثّقان بـ`CLAUDE.md` طُبّقا حرفياً:** (أ) تحقّق `information_schema.columns` بعد الاستبدال أكّد الأعمدة الخمسة بنفس الاسم والترتيب (٣ = `owner_repeat_pct` زي ما كانت)؛ (ب) `create or replace view` صفّر `security_invoker` كما متوقّع — `alter view ... set (security_invoker = on)` بنفس الـmigration، وتحقّق `select reloptions from pg_class where relname='v_kpi_quality'` رجع `{security_invoker=on}`.
  - **فحص الحماية:** `set local role authenticated` بـ`jwt.claims` لمستخدم وهمي غير موظّف (`is_staff()` بيرجّع false) رجّع صفاً واحداً **بكل القيم `null`** — مش "صفر صفوف" حرفياً (هاي view بترجع صف واحد ثابت من subqueries عددية، مش جدول صفوف)، لكنها نفس الإشارة: كل الأعمدة الخمسة (حتى اللي ما بتلمس `profiles` أصلاً زي `field_verified_pct`) رجعت `null` لنفس السبب — سياسات `staff_read` على `listings`/`contact_requests`/`reports`/`profiles` (الأربعة، تحقّق حي بـ`pg_policies`) بتحجب كل صف عن غير الموظف، فكل الـsubqueries الفرعية بترجع صفر صف وتتحوّل لـ`null` عبر `nullif(...,0)`. **التحقّق من الجدول تحت الـview كافٍ هون، مش مطلوب فحص إضافي خاص بـ`profiles`** لأنها محكومة بنفس النمط بالضبط.
  - **إثبات حي إن التجميع صحيح:** إنشاء إعلانين وهميين بنفس رقم الهاتف (`0599000222`) باسمين مختلفين عبر `submit_listing` (بعد إصلاحها الأخير، كل استدعاء ياخد `owner_id` مختلف فعلاً) — `owner_repeat_pct` قفز من `0.0` لـ`25.0`، يعني التكرار انضبط بالهاتف رغم اختلاف `owner_id`. البيانات الوهمية (٢ إعلان، ٢ بروفايل، صفوف `events`) انحذفت بعدها، والمؤشر رجع لقيمته الحقيقية `0.0` (لا تكرار فعلي حالياً بالبيانات — النتيجة نفسها قبل وبعد التعديل، بس الآن صحيحة منطقياً لا صدفة).
  - توثيق: تحديث سطر «الحسابات» بـ`CLAUDE.md` (كان بيوصف الأثر كـ"بيقلّل تعداد" بصيغة المستقبل، صار يشير للإصلاح الفعلي)، وإضافة شرح مفصّل تحت «السياق التجاري» بجانب تعريف المؤشر نفسه — يوضّح سبب التجميع بالهاتف، ولماذا آمن هون تحديداً رغم منعه بالكتابة بمكان تاني، مع تحذير صريح **ممنوع التحويل رجوعاً لـ`owner_id`**.
- **الخطوة الجاية:** لا شيء معلّق على هالإصلاح.

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة رابعة) — إصلاح ثغرة انتحال بـ`submit_listing`: إلغاء الربط التلقائي بمطابقة الهاتف
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - قُرئ تعريف `submit_listing` الحي أولاً (`pg_get_functiondef`) بدل الاعتماد على الذاكرة أو `CLAUDE.md`: كانت تعمل `select id from profiles where phone = trim(p_phone) and role='owner' limit 1` وتعيد استخدام أي صف موجود كـ`owner_id` للإعلان الجديد — نفس ثغرة الانتحال اللي انسدّت سابقاً بـ`link_account_role()` (الهاتف مش مُتحقَّق منه بأي OTP، فأي حدا يعرف رقم مالك حقيقي يقدر ينشر إعلاناً منسوباً لبروفايله).
  - **تقييم الخيارين المطروحين قبل الاختيار:** (أ) إلغاء إعادة الاستخدام نهائياً — صف `profiles` جديد بكل استدعاء؛ (ب) إبقاء إعادة الاستخدام بس لما الإعلان يُنشأ عبر رابط مالك موقّع أو جلسة `authenticated`. تحقّق عملي حسم الاختيار: `submit_listing` بتُستدعى حصراً من `index.html` (`grep` على كل `public/`، استدعاء وحيد)، ودايماً بمفتاح `anon` الثابت بالهيدر (`Authorization: Bearer <anon key>`) — ما في أي مسار حالي يمرّر توكن جلسة مستخدم حقيقي، و`owner.html` (الرابط الموقّع) أصلاً ما فيها نموذج إنشاء إعلان جديد، بس عرض/إدارة الموجود. يعني الخيار (ب) كان بده يتطلّب بنية تحتية جديدة بالكامل (تمرير JWT من `index.html`، أو نموذج إنشاء إعلان جديد بـ`owner.html`) — بالضبط النوع اللي الطلب منع اختراعه صراحة. اخترت **الخيار (أ)**.
  - **التنفيذ** (migration `20260914204457_submit_listing_no_phone_owner_reuse`): `create or replace function submit_listing(...)` — حُذف الـ`select`/`if v_owner is null` بالكامل، صار `insert into profiles (...) returning id into v_owner` ينفّذ دايماً بلا شرط. التوقيع (أسماء/أنواع/ترتيب المعطيات) بدون أي تغيير، فما احتجت `drop function` ولا إعادة `grant` (فخ التوقيعات المتعددة بجدول التشخيص ما ينطبق هون).
  - **فحص حي:** استدعاءان متتاليان بنفس رقم الهاتف (`0599000111`) باسمين مختلفين ("مالك اختبار أ" ثم "منتحل") أنتجا `owner_id` مختلف تماماً لكل واحد (تأكّد بـ`select ... join profiles`). البيانات الوهمية (٢ إعلان، ٢ بروفايل، صفوف `events` المرافقة) انحذفت بعدها وتأكّد الحذف. `has_function_privilege('anon'/'authenticated', ..., 'execute')` = `true` للاثنين بعد التعديل — مسار بدون تسجيل دخول لسا شغّال بدون أي قيد إضافي.
  - **الأثر المقبول:** ملّاك بيكرروا التسجيل بدون حساب هيحصلوا على صف `profiles` جديد (`owner_id` مختلف) كل مرة بدل إعادة استخدام صفهم القديم. انعكاسان بمركز التحكم: (أ) `v_admin_owners` هيعرض عدة صفوف لنفس الشخص الفعلي (نفس الهاتف ظاهر بعمود `phone` بكل صف، فالطاقم يقدر يميّزها بصرياً)، وكل صف بياخد `listings_total`/`fees_due`/إلخ خاصة فيه بس، مش مجمّعة؛ (ب) `v_kpi_quality.owner_repeat_pct` (مؤشر PMF الأساسي بالمشروع) بيحسب التكرار بتجميع `listings.owner_id` — يعني ملّاك متكررين بدون حساب هيبانوا "أول مرة" كل مرة، فالمؤشر هينخفض عن الواقع لحد ما يفعّلوا حساب (`account_uid`) أو الطاقم يدمج يدوياً. **لا يوجد أداة دمج تلقائي لصفوف مكررة** — `admin_link_profile()` بتربط صف واحد بحساب Auth موجود، ما بتنقل إعلانات صف لصف تاني.
  - توثيق: أُضيف سطر بقسم «الحسابات» بـ`CLAUDE.md` يوصل مبدأ `link_account_role` بـ`submit_listing` (نفس المبدأ، نفس السبب)، ويوثّق الأثر على `owner_repeat_pct` وغياب أداة الدمج.
- **قرار اتخذته بنفسي دون الرجوع:** لم أبنِ أداة دمج/تجميع صفوف مكررة رغم إنها فجوة واضحة نتجت عن الإصلاح — خارج نطاق الطلب الصريح (إصلاح الثغرة فقط)، وتحتاج قرار تصميم منفصل (دمج تلقائي بالهاتف بيرجّع نفس مخاطر الانتحال، ودمج يدوي يحتاج واجهة إدارية جديدة).
- **الخطوة الجاية:** لا شيء معلّق على هالإصلاح. لو تكرار البروفايلات المكررة صار ملحوظاً بلوحة `v_admin_owners`، القرار المؤجّل هو بناء أداة دمج يدوي صريحة (`admin_merge_owner_profiles()` أو ما شابه) — لسا ما انطلب.

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة ثالثة) — إعادة تسمية «سكنّا» مرحلة ٢: نصوص الواجهة + محتوى صفحات القاعدة + توثيق + نشر
- **آخر commit:** `c5e1276` (توثيق) — `a7c7433` (نصوص الواجهة) قبله بالسجل نفسه
- **شو صار:**
  - **مرحلة أ (نصوص الواجهة):** `grep -rn "موضع\|Mawdi\|MAWDI\|mawdi" public/ src/` قبل البدء طلع ٩٥ تطابق بالملفات الستة العامة + ملفين بـ`src/` ما كانا بنطاق الطلب الأصلي لكن يدخلان بشرط الـgate (`src/worker.js` — عنوان/وصف `og:` لبطاقة مشاركة الإعلان، ظاهر للعامة فعلياً؛ `src/whatsapp.js` — أسماء قوالب واتساب بتعليقات، غير مستدعاة من أي مكان بعد فحُسمت التسمية بأمان). استُبدلت بأربع تمريرات مرتّبة لكل ملف (`MAWDI`→`SAKANNA`، `Mawdi`→`Sakanna`، `mawdi`→`sakanna`، `موضع`→`سكنّا`) عبر Edit `replace_all`. الـgate بعدها: صفر تطابق بالاثنين.
  - **قرار لغوي:** سطر hero بـ`index.html` ("ابحث عن موضعك.") كان تلاعباً لفظياً بمعنى "مكان" لا استخداماً لاسم العلامة — استبدال حرفي كان رح ينتج "سكنّاك" (خطأ نحوي، اسم علم ما بياخد ضمير متصل). استُبدل بـ"ابحث عن سكنك." مستخدماً المصطلح العام «سكن» (قاعدة ٢٢).
  - مفاتيح تخزين إضافية غير مذكورة صراحة بالطلب لكن لازمة لتصفير الـgate: `mawdi_seen`→`sakanna_seen` (`sessionStorage`)، `mawdi_listing_draft`→`sakanna_listing_draft`، `mawdi_session`→`sakanna_session`، وملفات تصدير CSV بمركز التحكم `mawdi-listings.csv`/`mawdi-fees.csv`→`sakanna-*`.
  - **مرحلة ب (فحص عرض الشعار موبايل ≈٣٨٠px):** أداة تغيير حجم النافذة ما اشتغلت بهالبيئة (نافذة كروم بقيت بحجمها الكامل رغم نجاح الاستدعاء ظاهرياً — تأكّد بـ`window.innerWidth` بعد الاستدعاء). البديل: حقن `<style>` يقيّد `html,body{width:380px}` (بدون أي media query على الهيدر بأي من الملفات الستة — تأكّد بـ`grep @media` — فالتقييد بعرض الصندوق يكافئ فيوبورت حقيقي هون) + قياس `getBoundingClientRect` مباشر. النتيجة على الأربع صفحات اللي فيها شعار لاتيني تحت الاسم العربي (`index`/`account`/`page`/`institutions`): صفر تجاوز أفقي بأي صفحة. عرض كلمة "SAKANNA" الفعلي (٦٤.٧–٦٧.٨px حسب حجم الخط لكل ملف) طلع **أضيق** من "MAWDI" القديم (٧٠.٧px بنفس القياس على `index.html`) — تأكّد بتبديل النص مؤقتاً بالمتصفح الحي وإعادة القياس، بدون لمس أي ملف. التفاف صف أزرار الهيدر لسطر ثانٍ على `index.html`/`account.html` موجود مسبقاً بغض النظر عن طول الاسم (نفس `sameRow:false` بالاسم القديم والجديد) — تصميم `flex-wrap` قائم أصلاً، مش تراجع من التسمية. **لا حاجة لأي تعديل CSS.**
  - **مرحلة ج (محتوى `pages`):** استُعلم الـschema الحي أولاً (`information_schema.columns` + `pg_get_function_identity_arguments` على `admin_page_save`) قبل أي افتراض — الأعمدة: `key`/`title_ar`/`body_md`/`is_published`/`updated_at`/`updated_by`/`title_en`/`body_en`. التعديل عبر `admin_page_save` حصراً (لا كتابة مباشرة على الجدول)، باستخدام `replace()` على القيم الحيّة نفسها داخل استعلام SQL (`select admin_page_save(key, replace(title_ar,'موضع','سكنّا'), ...) from pages where key=...`) بدل إعادة كتابة النص يدوياً — يلغي مخاطرة خطأ نسخ بنص طويل. الصفحتان `how-it-works`/`privacy` (٧ تطابقات موضع/Mawdi إجمالاً بينهما) استبدال مباشر آمن (تأكّد ما في استخدام "موضع" بمعنى مكان بأي منهما). صفحة `terms`: العبارة "موضع منصة **وساطة** وتوثيق" (وبالإنجليزي "matchmaking and verification platform") كانت تُصنّف المنصة كوسيط — بالتناقض مع الموقف القانوني الفعلي (خدمة تقنية وتوثيق، والرسم رسم خدمة توثيق وإدراج لا عمولة وساطة). استُبدلت بـ"سكنّا منصة تقنية توثّق إعلانات السكن وتربط الباحث بالمالك" / "Sakanna is a technology platform that verifies listings and connects seekers with owners" — بدون "وساطة"/"matchmaking"، بفصحى بسيطة (قاعدة ٢٣). `updated_by` بكل الصفوف صار `claude-rebrand` (مرّر كـ`p_actor` — يُستخدم فقط لأن `auth.uid()` فارغ باستدعاء SQL مباشر، قاعدة ١٦)، وسُجّل بـ`admin_actions` (`action='page_save'`, `actor='claude-rebrand'`, ثلاث صفوف). الـgate: `~ 'موضع|Mawdi|وساطة'` على دمج الحقول الأربعة لكل صف = `false` بالثلاثة.
  - **مرحلة د (توثيق):** `CLAUDE.md` — جدول البنية كان لسا يوثّق `mawdi_lang` رغم تغييره فعلياً؛ قاعدة ٦ كانت تدّعي `/admin`=`200` (تحقّق `curl` حي أظهره `307`→`/admin/`); قاعدة ١٦ كانت تحصر بقاء `p_actor` بثلاث دوال فقط — استعلام حي (`pg_get_function_identity_arguments` على كل `admin_%`) أظهر **خمس عشرة** دالة لسا شايلاه، مش ثلاث (القائمة الكاملة أُضيفت). الثلاث مغالطات صُحّحت بـcommit منفصل (`c5e1276`) — مش جزء من إعادة التسمية نفسها لكن اكتُشفت أثناء الفحص المطلوب بهالمرحلة.
  - **مرحلة هـ (نشر):** `git push` + `npx wrangler deploy` (٦ ملفات جديدة/معدّلة اتحمّلت: `page.html`/`account.html`/`institutions.html`/`index.html`/`owner.html`/`admin/index.html`). تحقّق حي بعد النشر: الجذر `200` وعنوانه الحقيقي صار "سكنّا — سكن موثّق..."، `/admin` `307`→`Location: /admin/`، و`/admin/` نفسها `200` مع `X-Robots-Tag: noindex, nofollow` على الاثنين.
- **قرارات اتخذتها بنفسي دون الرجوع:** (١) صياغة سطر hero بـ"سكنك" بدل استبدال حرفي مكسور نحوياً؛ (٢) توسيع نطاق الاستبدال لمفاتيح تخزين/أسماء ملفات وملفي `src/` غير مذكورين صراحة، لأن الـgate المطلوب صفره كان يغطيهم؛ (٣) إعادة صياغة الوساطة بالإنجليزي كمان رغم إن الطلب ذكر العربي فقط، لاتساق الموقف القانوني باللغتين؛ (٤) تصحيح مغالطة p_actor بقاعدة ١٦ رغم إنها غير مرتبطة بالتسمية، لأنها ظهرت أثناء الفحص المطلوب صراحة بمرحلة د.
- **الخطوة الجاية:** لا شيء معلّق على إعادة التسمية — المراحل الخمس كلها منفّذة ومنشورة ومتحقَّق منها حياً. المتبقي من الجلسات السابقة كما هو (SMTP مخصّص، ربط بروفايل قديم، دخول OTP، تجربة دخول طاقم حقيقي).

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة) — إكمال وتدقيق نموذج الرسم حسب نوع السكن (مراحل ٣-٦)
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - جلسة سابقة نفس اليوم (غير موثّقة بـ`STATUS.md`) كانت خلصت فعلياً معظم بناء «الرسم حسب نوع السكن»: `quote_listing_fee(p_kind)` (`ba2ac55`)، إعادة كتابة `compute_fee_due()` لتقرأ منها (`31851ee`)، `admin_fee_amount()` للتعديل اليدوي (`2e00d7a`)، وعرض الرسم الفعلي + كود الخصم النشط بنموذج «أضف غرفة» (`7a53c9c`). هالجلسة كانت تدقيق واستكمال على هالبناء، مش بناء من الصفر
  - **تدقيق `compute_fee_due()` (migration `20260914154212`):** استُعلم الـschema الحيّ قبل أي قرار — `owner_fees.listing_id` طلع `NOT NULL` + `FOREIGN KEY ... ON DELETE CASCADE` لـ`listings(id)`، يعني ما في احتمال صف رسم بدون إعلان مرتبط أصلاً. وتريغر `trg_fee_due` طلع `BEFORE INSERT OR UPDATE`، والمسار الوحيد للإدراج (`on_contact_rented()`) بيعبّي `listing_id` بنفس جملة الإدراج، يعني `new.listing_id` متاح وقت ما الدالة تشتغل. **القرار: لا حاجة لإعادة كتابة الدالة** — النسخة الموجودة أصلاً بتقرأ `amount_base` من `quote_listing_fee(l.kind)` مع `coalesce` احتياطي لـ`fee_base`، وهذا يغطي الحالتين المطلوبتين فعلياً (حتى لو نظرياً غير ممكنتين حالياً بحكم القيود). اختبار حي (`gate`): صف شقة بدون كود = ٥٠٠، صف استوديو بكود `SAKANNA50` = ١٥٠ — نجح، والبيانات الوهمية (صفّا `owner_fees` + إعلانان وهميان) انحذفت والعدّاد رجع صفر
  - **تدقيق `admin_fee_amount()`:** الحارس والمنح مطابقة للمطلوب تماماً (`is_staff() or auth.uid() is null`، بدون `p_actor`، `revoke ... from public` ثم `grant ... to authenticated, service_role`). `has_function_privilege('anon', ...)` = `false` — تأكّد
  - **تدقيق الواجهة (`index.html`):** الكود الموجود من `7a53c9c` مطابق حرفياً للنص المطلوب («رسم النجاح: [X] شيكل — يُدفع مرة واحدة بعد تأكيد التأجير...») وعرض كود الخصم النشط بسطر منفصل عبر `active_promo()`، بدون أي حساب بالجافاسكربت (تأكّد بـ`grep`). **لكن الكود هذا لسا غير منشور على الإنتاج** — فحص `curl` مباشر على `sakan.abdallatif-tiyah.workers.dev` أظهر عدم وجود `renderFeeInfo`/`quote_listing_fee` بالصفحة الحيّة (النشر يدوي، `wrangler deploy` ما انشغّل من بعد الـcommit). بدل ما أنشر تعديل واجهة غير مُختبر على الإنتاج، شغّلت خادم استاتيكي محلي (`python -m http.server`) وفتحته بمتصفح فعلي: تأكّد الرسم يتغيّر صح (غرفة بسكن مشترك=١٠٠، شقة=٥٠٠) وسطر الخصم يظهر بالاثنين — لقطتا شاشة محفوظتان محلياً
  - **فجوتان اكتُشفتا وانسدّتا:** (أ) قيم `settings` الخمس (`fee_apartment=500`، `fee_studio=300`، `fee_room_shared=100`، `fee_bed_shared=100`، `fee_family=500`) كانت متطابقة بتاريخ تحديث واحد لا يقابله أي migration — أُضيف `20260914193031_seed_fee_settings_by_kind` (`insert ... on conflict (key) do nothing`) لإعادة إنتاجها على قاعدة جديدة. (ب) كود الخصم `MAWDI50` كان انبدّل لـ`SAKANNA50` مباشرة بالقاعدة بدون أي migration — أُضيف `20260914193624_rebrand_promo_code_sakanna50` (`update ... where code='MAWDI50'`)
  - **توثيق `CLAUDE.md`** (كان معدَّل بالفعل بشجرة العمل من الجلسة السابقة غير الموثّقة، رَاجعته وتركته كما هو لأنه مطابق تماماً): حُذف ادّعاء «نصف أجرة شهر» الخاطئ (الرسم مبلغ ثابت حسب النوع لا نسبة)، `quote_listing_fee` موثّقة كمصدر وحيد، `admin_fee_amount` أُضيفت لقائمة الدوال الإدارية (العدد صار ٢٢)، وقاعدة ٢٢ اتعدّلت لتوضّح إن `bed_shared`/`family` موجودتان بالـenum ومسعّرتان بس مش بمنتقي الواجهة
  - `npx supabase migration list` بعد كل تطبيق طابق المحلي بالبعيد بالحرف (٥٣ migration، آخرهم `20260914193624`)
  - فحص أمني (`get_advisors`) بعد كل التعديلات: لا نتائج جديدة — كل التنبيهات (`SECURITY DEFINER` على دوال `anon`/`authenticated`) نتيجة متوقّعة لتصميم قائم أصلاً (كل RPC عام محروس بحارس داخلي، موثّق بـ`CLAUDE.md`)، مش ثغرة من هالجلسة
- **قرار اتخذته بنفسي دون الرجوع:** ما شغّلت `wrangler deploy` — نشر كود واجهة غير مُختبر مباشرة على الإنتاج فعل له أثر عام (رؤية المستخدمين)، فاخترت التحقق بخادم محلي بدل المخاطرة، وتركت النشر الفعلي قرار صريح للاحقاً
- **الخطوة الجاية:** `npx wrangler deploy` لنشر عرض الرسم الفعلي + كود الخصم بنموذج «أضف غرفة» (الكود بالريبو من `7a53c9c`، غير منشور بعد)، ثم استئناف مرحلة ٢ من إعادة تسمية «سكنّا» (تعديل كود HTML الفعلي — لسا ما بدأت)

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ — رحلة تسمية: تغيير بادئة مرجع الإعلان `MW-` ← `SK-` وتصفير العدّاد، ثم إعادة تسمية «موضع/Mawdi» ← «سكنّا/Sakanna» (مرحلة ١ — توثيق فقط)
- **آخر commit:** `abe2ed1` (تغيير البادئة — قبل هالجلسة) — commit هالجلسة نفسها بينعكس بالسجل الجاي
- **شو صار:**
  - بادئة مرجع الإعلان `set_listing_ref()`/`listing_ref_seq` رجعت لـ`SK-` عمداً (استثناء موثّق رغم إعادة التسمية العامة) — migration `20260914145004_listing_ref_prefix_sk`، العدّاد أُعيد ضبطه لـ١٠٠١
  - **مرحلة ١ من إعادة التسمية «سكنّا» (Sakanna):** توثيق فقط بـ`CLAUDE.md`/`STATUS.md` — لا تعديل على أي ملف HTML ولا القاعدة. استُبدلت كل إشارات «موضع»/Mawdi/`MAWDI50`/`mawdi_lang` بـ«سكنّا»/Sakanna/`SAKANNA50`/`sakanna_lang` بملفات التوثيق (فقرة الاسم، الهوية البصرية، السياق التجاري، سجل الجلسات). استثناء متروك عمداً: وصف `index.html` بجدول «البنية» بـ`CLAUDE.md` بيذكر `mawdi_lang` لأنه بيوثّق سلوك الكود الفعلي الحالي غير المعدَّل بعد — هيتحدّث بمرحلة لاحقة لما الكود نفسه يتغيّر
  - المراحل الجاية (كود HTML + القاعدة) لسا ما بدأت — بانتظار تعليمات صريحة
- **الخطوة الجاية:** مرحلة ٢ — تعديل الكود الفعلي (`index.html`/`page.html`/`owner.html`/`institutions.html`/`account.html`) لاستبدال «موضع» بـ«سكنّا» ومفتاح `localStorage` من `mawdi_lang` لـ`sakanna_lang` (كود الخصم بالقاعدة انبدّل فعلاً لـ`SAKANNA50` — شوف الجلسة أعلاه)

---

## مهام مفتوحة — بترتيب الأولوية

- [ ] **مزوّد بريد مخصّص + إعادة تفعيل تأكيد البريد** — مرتبط بربط النطاق. الحل المؤقت الحالي (`Confirm email` مطفأ) يفتح التسجيل الفوري بدون بريد إطلاقاً، وهذا مقصود مؤقتاً فقط — أي مستخدم يُنشئ حساباً بأي بريد حتى لو غير مملوك له فعلياً، لأنه ما في تحقّق. عالج قبل أي إعلان علني عن الحسابات
- [ ] قرار مؤجّل: **ربط البروفايل القديم بالتوكن الموقّع** — حالياً المالك صاحب الإعلانات ينشئ حساباً فيحصل على بروفايل فارغ (`link_account_role` ما بتربط بمطابقة هاتف عمداً)، والربط يدوي بـ`admin_link_profile`. يُبنى حين يتكرر الربط اليدوي أكثر من مرة أسبوعياً
- [ ] قرار مؤجّل: **دخول برمز لمرة واحدة (OTP)** بجانب كلمة السر لا بدلاً منها — قابل للتراجع، عائد لصاحب المشروع
- [ ] حذف حسابات Auth التجريبية (`abdallatif.tiyah+audit-*`/`+livetest-*@gmail.com`) يدوياً من `/auth/users` إذا حبيت قائمة نظيفة — ما انحذفت برمجياً (لا أداة Auth Admin API متاحة بالجلسة)
- [ ] تجربة دخول فعلي بحساب طاقم حقيقي على `/admin` (البوابة نفسها تأكّدت، الدخول الفعلي لسا لأ)
- [ ] فجوة العرض ببيرزيت — مناطق معرّفة بالقاعدة، المخزون معدوم، والطلب غير مُتحقَّق منه ميدانياً بعد — كل بيانات الموقع تجريبية
- [ ] تعيين وتجهيز المندوب قبل ذروة الموسم
- [ ] تعبئة `name_en` يدوياً للمدن/المناطق الجديدة لما تنضاف (الموجودة معبّاة)
- [ ] استكمال التسجيل بوزارة الاقتصاد الوطني — شرط لفوترة المؤسسات
- [ ] قرار مستقبلي: إعادة تصميم بصرية كاملة لمركز التحكم (بقيت بتوكناتها القديمة عمداً هالجلسة)
- [ ] بعد بوابة القرار: ربط النطاق `sakan.ps` — بيد أبواللطيف، خارج نطاق Claude

---

## سجل الجلسات (آخر ٥ — السادسة بتنحذف، ما في أرشفة)

| تاريخ | الجلسة | commit |
|---|---|---|
| ٢٠٢٦-٠٩-١٤ | إصلاح `owner_repeat_pct` (`v_kpi_quality`) — تجميع بـ`profiles.phone` بدل `owner_id` بعد أثر إصلاح `submit_listing` الجانبي | (هالجلسة) |
| ٢٠٢٦-٠٩-١٤ | إصلاح ثغرة انتحال بـ`submit_listing` — إلغاء الربط التلقائي بمطابقة الهاتف (نفس مبدأ `link_account_role`) | `62ee504` |
| ٢٠٢٦-٠٩-١٤ | إعادة تسمية «سكنّا» مرحلة ٢: نصوص الواجهة الستة + `src/`، محتوى صفحات القاعدة عبر `admin_page_save`، تصحيح ثلاث مغالطات بـ`CLAUDE.md`، نشر ومتحقَّق منه حياً | `a7c7433`/`c5e1276` |
| ٢٠٢٦-٠٩-١٤ | تدقيق واستكمال نموذج الرسم حسب نوع السكن (مراحل ٣-٦: تحقّق `compute_fee_due`/`admin_fee_amount`، اختبار واجهة محلي، migrations لتثبيت `settings`/`SAKANNA50`) | `30ba41e` |
| ٢٠٢٦-٠٩-١٤ | تغيير بادئة المرجع `MW-`←`SK-` + تصفير العدّاد، ثم إعادة تسمية «موضع» ← «سكنّا» (مرحلة ١ توثيق) | `abe2ed1`/`892cb1e` |
| ٢٠٢٦-٠٩-٠٦ | إعادة بناء `owner.html` كاملة + `owner_contact_status` + إصلاح باگين حيّين | `b671278` |
