# STATUS — الحالة المتحركة

للثابت (بنية · صلاحيات · قواعد · تشخيص) شوف `CLAUDE.md`.
هذا الملف بينحدّث بآخر كل جلسة. لا تكتب فيه أي شي مستنتج من أمر — اكتب الأمر.

---

## الحالة الآن

| البند | الحالة |
|---|---|
| قاعدة | متزامنة — تحقّق: `npx supabase migration list` (المحلي لازم يطابق البعيد بالحرف) |
| ويب | منشور على `sakan.abdallatif-tiyah.workers.dev`. هوية بصرية جديدة (أزرق/كهرماني) + عربي/إنجليزي كامل على `index.html` · `page.html` · `institutions.html` · `account.html`. **`owner.html` عربي بس عمداً** (رابط موقّع خاص، بدون مبدّل لغة — قرار مقفول، شوف `CLAUDE.md`). مركز التحكم عربي بس، وتوكناته القديمة لسا زي ما كانت عمداً (قرار متحفّظ، شوف `CLAUDE.md`). النشر يدوي: `npx wrangler deploy` |
| حسابات | مفعّلة وإضافية — `public/account.html`. مالك/باحث يقدر يفعّل صفة أو الاثنتين فوق حساب واحد. إشعارات تلقائية (طلب تواصل، تغيّر حالة/توثيق، قرب الانتهاء، نشر طلب، تطابق جديد) + جرس بمركز التحكم لإرسال يدوي. الفحص الأمني مرّ كامل (منهجية `set local role` بدل مستخدم Auth حقيقي — لا أداة API متاحة لإنشاء/حذف مستخدم اختبار)، بالإضافة لاختبار حي فعلي على الموقع المنشور (تسجيل → دخول فوري → تفعيل صفة → إشعار). **`Confirm email` مطفأ حالياً** (خدمة البريد المدمجة كانت ترفض التسليم لغير أعضاء الفريق أصلاً) — يعني **لا تحقّق من ملكية البريد عند التسجيل مؤقتاً**، أي حد يقدر يسجّل بأي بريد. إعادة التفعيل مرتبطة بربط SMTP مخصّص (شوف «مهام مفتوحة») |
| بيانات | لا تعتمد على أرقام محفوظة هون. استعلم مباشرة: `select count(*) from listings where status='published'` · `select status, count(*) from seeker_requests group by 1` · `select count(*) from profiles` · `select count(*) from institution_leads` · `select count(*) from owner_fees` · `select count(*) from profiles where account_uid is not null` |
| واتساب API | مجهّز بالكود، غير مفعّل — محتاج حساب Meta Business حقيقي |
| تسجيل الشركة | التسجيل بوزارة الاقتصاد الوطني (قانون التجارة الإلكترونية ٢١/٢٠٢٥) لسا ما خلص — بدونه ما في فوترة للمؤسسات |

---

## آخر جلسة

- **التاريخ:** ٢٠٢٦-٠٩-١٤ (متابعة) — إكمال وتدقيق نموذج الرسم حسب نوع السكن (مراحل ٣-٦)
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - جلسة سابقة نفس اليوم (غير موثّقة بـ`STATUS.md`) كانت خلصت فعلياً معظم بناء «الرسم حسب نوع السكن»: `quote_listing_fee(p_kind)` (`ba2ac55`)، إعادة كتابة `compute_fee_due()` لتقرأ منها (`31851ee`)، `admin_fee_amount()` للتعديل اليدوي (`2e00d7a`)، وعرض الرسم الفعلي + كود الخصم النشط بنموذج «أضف غرفة» (`7a53c9c`). هالجلسة كانت تدقيق واستكمال على هالبناء، مش بناء من الصفر
  - **تدقيق `compute_fee_due()` (migration `20260914154212`):** استُعلم الـschema الحيّ قبل أي قرار — `owner_fees.listing_id` طلع `NOT NULL` + `FOREIGN KEY ... ON DELETE CASCADE` لـ`listings(id)`، يعني ما في احتمال صف رسم بدون إعلان مرتبط أصلاً. وتريغر `trg_fee_due` طلع `BEFORE INSERT OR UPDATE`، والمسار الوحيد للإدراج (`on_contact_rented()`) بيعبّي `listing_id` بنفس جملة الإدراج، يعني `new.listing_id` متاح وقت ما الدالة تشتغل. **القرار: لا حاجة لإعادة كتابة الدالة** — النسخة الموجودة أصلاً بتقرأ `amount_base` من `quote_listing_fee(l.kind)` مع `coalesce` احتياطي لـ`fee_base`، وهذا يغطي الحالتين المطلوبتين فعلياً (حتى لو نظرياً غير ممكنتين حالياً بحكم القيود). اختبار حي (`gate`): صف شقة بدون كود = ٥٠٠، صف استوديو بكود `SAKANNA50` = ١٥٠ — نجح، والبيانات الوهمية (صفّا `owner_fees` + إعلانان وهميان) انحذفت والعدّاد رجع صفر
  - **تدقيق `admin_fee_amount()`:** الحارس والمنح مطابقة للمطلوب تماماً (`is_staff() or auth.uid() is null`، بدون `p_actor`، `revoke ... from public` ثم `grant ... to authenticated, service_role`). `has_function_privilege('anon', ...)` = `false` — تأكّد
  - **تدقيق الواجهة (`index.html`):** الكود الموجود من `7a53c9c` مطابق حرفياً للنص المطلوب («رسم النجاح: [X] شيكل — يُدفع مرة واحدة بعد تأكيد التأجير...») وعرض كود الخصم النشط بسطر منفصل عبر `active_promo()`، بدون أي حساب بالجافاسكربت (تأكّد بـ`grep`). **لكن الكود هذا لسا غير منشور على الإنتاج** — فحص `curl` مباشر على `sakan.abdallatif-tiyah.workers.dev` أظهر عدم وجود `renderFeeInfo`/`quote_listing_fee` بالصفحة الحيّة (النشر يدوي، `wrangler deploy` ما انشغّل من بعد الـcommit). بدل ما أنشر تعديل واجهة غير مُختبر على الإنتاج، شغّلت خادم استاتيكي محلي (`python -m http.server`) وفتحته بمتصفح فعلي: تأكّد الرسم يتغيّر صح (غرفة بسكن مشترك=١٠٠، شقة=٥٠٠) وسطر الخصم يظهر بالاثنين — لقطتا شاشة محفوظتان محلياً
  - **فجوتان اكتُشفتا وانسدّتا:** (أ) قيم `settings` الخمس (`fee_apartment=500`، `fee_studio=300`، `fee_room_shared=100`، `fee_bed_shared=100`، `fee_family=500`) كانت متطابقة بتاريخ تحديث واحد لا يقابله أي migration — أُضيف `20260914193031_seed_fee_settings_by_kind` (`insert ... on conflict (key) do nothing`) لإعادة إنتاجها على قاعدة جديدة. (ب) كود الخصم `MAWDI50` كان انبدّل لـ`SAKANNA50` مباشرة بالقاعدة بدون أي migration — أُضيف `20260914193624_rebrand_promo_code_sakanna50` (`update ... where code='MAWDI50'`)
  - **توثيق `CLAUDE.md`** (كان معدَّل بالفعل بشجرة العمل من الجلسة السابقة غير الموثّقة، رَاجعته وتركته كما هو لأنه مطابق تماماً): حُذف ادّعاء «نصف أجرة شهر» الخاطئ (الرسم مبلغ ثابت حسب النوع لا نسبة)، `quote_listing_fee` موثّقة كمصدر وحيد، `admin_fee_amount` أُضيفت لقائمة الدوال الإدارية (العدد صار ٢٢)، وقاعدة ٢٢ اتعدّلت لتوضّح إن `bed_shared`/`family` موجودتان بالـenum ومسعّرتان بس مش بمنتقي الواجهة
  - `npx supabase migration list` بعد كل تطبيق طابق المحلي بالبعيد بالحرف (٥٣ migration، آخرهم `20260914193624`)
  - فحص أمني (`get_advisors`) بعد كل التعديلات: لا نتائج جديدة — كل التنبيهات (`SECURITY DEFINER` على دوال `anon`/`authenticated`) نتيجة متوقّعة لتصميم قائم أصلاً (كل RPC عام محروس بحارس داخلي، موثّق بـ`CLAUDE.md`)، مش ثغرة من هالجلسة
- **قرار اتخذته بنفسي دون الرجوع:** ما شغّلت `wrangler deploy` — نشر كود واجهة غير مُختبر مباشرة على الإنتاج فعل له أثر عام (رؤية المستخدمين)، فاخترت التحقق بخادم محلي بدل المخاطرة، وتركت النشر الفعلي قرار صريح للاحقاً
- **الخطوة الجاية:** `npx wrangler deploy` لنشر عرض الرسم الفعلي + كود الخصم بنموذج «أضف غرفة» (الكود بالريبو من `7a53c9c`، غير منشور بعد)، ثم استئناف مرحلة ٢ من إعادة تسمية «سكنّا» (تعديل كود HTML الفعلي — لسا ما بدأت)

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-١٤ — رحلة تسمية: تغيير بادئة مرجع الإعلان `MW-` ← `SK-` وتصفير العدّاد، ثم إعادة تسمية «موضع/Mawdi» ← «سكنّا/Sakanna» (مرحلة ١ — توثيق فقط)
- **آخر commit:** `abe2ed1` (تغيير البادئة — قبل هالجلسة) — commit هالجلسة نفسها بينعكس بالسجل الجاي
- **شو صار:**
  - بادئة مرجع الإعلان `set_listing_ref()`/`listing_ref_seq` رجعت لـ`SK-` عمداً (استثناء موثّق رغم إعادة التسمية العامة) — migration `20260914145004_listing_ref_prefix_sk`، العدّاد أُعيد ضبطه لـ١٠٠١
  - **مرحلة ١ من إعادة التسمية «سكنّا» (Sakanna):** توثيق فقط بـ`CLAUDE.md`/`STATUS.md` — لا تعديل على أي ملف HTML ولا القاعدة. استُبدلت كل إشارات «موضع»/Mawdi/`MAWDI50`/`mawdi_lang` بـ«سكنّا»/Sakanna/`SAKANNA50`/`sakanna_lang` بملفات التوثيق (فقرة الاسم، الهوية البصرية، السياق التجاري، سجل الجلسات). استثناء متروك عمداً: وصف `index.html` بجدول «البنية» بـ`CLAUDE.md` بيذكر `mawdi_lang` لأنه بيوثّق سلوك الكود الفعلي الحالي غير المعدَّل بعد — هيتحدّث بمرحلة لاحقة لما الكود نفسه يتغيّر
  - المراحل الجاية (كود HTML + القاعدة) لسا ما بدأت — بانتظار تعليمات صريحة
- **الخطوة الجاية:** مرحلة ٢ — تعديل الكود الفعلي (`index.html`/`page.html`/`owner.html`/`institutions.html`/`account.html`) لاستبدال «موضع» بـ«سكنّا» ومفتاح `localStorage` من `mawdi_lang` لـ`sakanna_lang` (كود الخصم بالقاعدة انبدّل فعلاً لـ`SAKANNA50` — شوف الجلسة أعلاه)

---

## آخر جلسة (سابقة)

- **التاريخ:** ٢٠٢٦-٠٩-٠٦ — إعادة بناء `owner.html` كاملة + `owner_contact_status`
- **آخر commit:** `b671278`
- **شو صار:**
  - إعادة بناء `public/owner.html` بالكامل: الطابور (طلبات `new` + إعلانات قرب الانتهاء) أول شي بالشاشة، كروت إعلانات كاملة (مش مضغوطة لسطور)، طلبات التواصل غير `new` تحت كل كرت إعلانه بشكل مضغوط، قسم رسوم جديد، بدون أي مبدّل لغة (الصفحة عربي بس عمداً — كانت أصلاً كذلك رغم إشارة `STATUS.md` القديمة الخاطئة لعكس ذلك، صُحّحت أعلاه)
  - migration واحد (`20260905225004_owner_dashboard_fees_and_contact_status`) بتعديلين: (أ) `owner_dashboard` — جسم فقط، أضاف مفتاح `fees` (قائمة `due`/`collected` فقط من `owner_fees`، بدون `amount_base`/`promo_code`/`collected_by`/`waived`/`lost`)؛ (ب) دالة جديدة `owner_contact_status(p_owner_id, p_token, p_contact_id, p_status)` — تتحقق توكن+ملكية+حظر، تسمح فقط بـ`owner_responded`/`viewing_set`/`dead`، ترفض `rented`/`forwarded`/`new` (rented حدث فوترة يبقى بيد الطاقم حصراً)، تسجّل بـ`events` بـ`source='user'`، `security definer`، ممنوحة لـ`anon`
  - زر «تم التأجير» **ما بينادي `owner_contact_status`** (كان سيُرفض) — بيعمل إدراج مباشر بـ`events` عبر REST مستفيد من منحة `anon` `insert`-فقط الموجودة أصلاً على الجدول (سياسة `anon_insert_event` تشترط `source='user'` فقط) — بدون أي دالة/منحة إضافية
  - الفحوص الأمنية الستة المطلوبة (توكن غلط، طلب تابع لمالك تاني، مالك محظور، `owner_dashboard` بدون حقول محظورة، `amount_due` حرفي بمبلغين مختلفين، `rented` مرفوض وبدون صف `owner_fees` جديد) نُفّذت كلها بـ`execute_sql` مباشر (مستخدمين/إعلانات/طلبات وهمية انحذفت بعدها) — نجحت كلها
  - **اختبار حي فعلي على الموقع المنشور** (بعد `wrangler deploy`، بمتصفح فعلي): اكتشف باگين حقيقيين وأصلحهما فوراً قبل التسليم:
    1. طلبات التواصل بحالة `new` كانت تظهر بالطابور فقط بدون أي زر فعل (لأن كرت الإعلان بيستثنيها عمداً) — يعني ما في طريقة فعلية يكشف فيها المالك رقم طلب جديد. الإصلاح: كرت الطابور نفسه صار فيه الرقم المموّه + زر «استلمت الطلب» مباشرة
    2. زر «تم التأجير» كان بيستبدل أول `.btn-row` بالكرت (صف اتصال/واتساب) بدل الصف اللي هو فيه فعلياً (لأن كرت الطلب الواحد فيه صفّا أزرار)، فكانت رسالة التأكيد بتحل مكان زر الاتصال الغلط والزر الأصلي يضل عالق على «جارِ الإرسال». الإصلاح: استهداف `btn.closest(".btn-row")` مباشرة
    - بعد الإصلاحين: تدفق الكشف الكامل (طابور → زر → رقم كامل + اتصال/واتساب) وتدفق «تم التأجير» (زر → رسالة تأكيد خضراء، بدون تغيير حالة فعلي) اتأكدا الاثنان يشتغلوا صح على الموقع الحي، وصف الـ`events` اتأكد بـSQL إنه انخلق بالشكل الصحيح
  - توثيق: أُضيفت `owner_contact_status` لجدول دوال `anon` بـ`CLAUDE.md`، وتوضيح إنه مبلغ الرسم متغيّر حسب نوع السكن ومصدره `owner_fees.amount_due` (بعد تريغر `compute_fee_due`) مش `settings.fee_base` مباشرة
  - **فحص إضافي قبل التسليم:** فحص الرسم المتغيّر (`owner_dashboard` بصفّي `owner_fees` وهميين، ٢٠٠ و٤٢٥ بعد خصم `SAKANNA50` على ٨٥٠، بالإضافة لصف `waived`) نُفّذ مباشرة على قاعدة الإنتاج عبر SQL وتحقّق بمخرَج `jsonb` خام — الاثنان فقط ظهرا بقيمتيهما الحرفيتين، بدون `amount_base`/`promo_code`، وصف `waived` غاب تماماً. البيانات الوهمية انحذفت وتأكّد بـ`select count(*)`.
  - **اكتُشف قبل النشر:** `wrangler deploy` بجلسة أمس (٢٠٢٦-٠٩-٠٥) صار **قبل** الـcommit، فبقي الإنتاج فيه نسخة `owner.html` بأربعة أسطر CSS تختلف عن الريبو المحلي (تأكّد بـ`diff` فعلي: `line-height` على `h1`، قاعدة `.queue .row`، حجمَي خط `11.5px` مقابل `12px` بـ`.queue .lbl`/`.req-date`، وسطر ارتفاع إضافي بـ`.btn.ghost`) — إلى جانب كل تعديلات هالجلسة نفسها (إعادة البناء الكاملة أعلاه) لسا غير منشورة أصلاً. صُحّح الترتيب هالمرة: commit أولاً ثم `wrangler deploy`.
- **الخطوة الجاية:** لا شيء معلّق على هالجزء. المتبقي من الجلسات السابقة زي ما هو (تجربة دخول طاقم حقيقي على `/admin`، القرارات المؤجّلة، SMTP مخصّص)

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-٠٥ (متابعة ثانية) — تشغيل التسجيل بدون تأكيد بريد بعد إطفاء `Confirm email`
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - المستخدم أطفأ `Confirm email` من لوحة Supabase يدوياً. تحقّقت بنفسي من مصدرين قبل أي تعديل (كما طُلب صراحة، وليس اعتماداً على القول): محاولة أولى أظهرت التبديل لسا شغّال فعلياً (لا `session` بالرد + لقطة لوحة تؤكد المفتاح أخضر) فأوقفت وأبلغت المستخدم بدل ما أكمل؛ بعد تأكيده أنه حفظ التبديل، أعدت الفحصين معاً (استدعاء `/auth/v1/signup` حقيقي رجع `access_token`/`session` كاملة + لقطة لوحة تؤكد المفتاح رمادي) — الاثنان متطابقان هالمرة.
  - `grep` فعلي على `account.html` أظهر أن `submitSignup()` **كانت أصلاً** تتحقق من `r.access_token` وتدخل فوراً لو موجود — لا حاجة لتغيير هالجزء، بس نص البديل (لما ما يرجع session) كان يفترض «تحقّق من بريدك» بشكل مطلق، فاستُبدل برسالة دفاعية تتعامل مع الحالتين (تعمل صح الآن، وتبقى معقولة لو انفعّل التأكيد لاحقاً).
  - **قاعدة الحذف على `profiles.account_uid`:** لا يوجد قيد مفتاح خارجي على العمود أصلاً (`select confdeltype from pg_constraint where conname like '%account_uid%'` رجع صفر صفوف) — فحذف مستخدم من لوحة Auth لن يحذف صف البروفايل تلقائياً (لا cascade)، لكنه بيسيب `account_uid` معلّق بلا مستخدم مطابق. لم ألمس شيئاً هون، توثيق فقط.
  - لا يوجد رابط/زر «نسيت كلمة السر» بالصفحة أصلاً (تحقّق بـ`grep`، صفر نتائج) — لا شيء لإخفائه. أُضيف بدلاً منه سطر «تعذّر الدخول؟ تواصل معنا» بتبويب الدخول، يوجّه لصفحة `index.html` ونموذج «تواصل معنا أو قدّم شكوى» الموجود أصلاً — بحثت بكل ملفات `public/` ومحتوى جدول `pages` ولا يوجد أي رقم واتساب دعم ثابت منشور بأي مكان (كل روابط `wa.me` ديناميكية لرقم المالك/الباحث)، فاستخدمت القناة المعتمدة الموجودة فعلاً بدل اختراع رقم.
  - **اختبار حي كامل على الموقع المنشور** (بعد `wrangler deploy`، بمتصفح فعلي لا SQL محاكى):
    - تسجيل حساب جديد (`abdallatif.tiyah+livetest-signup-1@gmail.com`) → **دخول فوري بدون أي شاشة انتظار**، اللوحة ظهرت مباشرة
    - تفعيل صفة مالك من الواجهة → `link_account_role` أنشأت صف `profiles` فعلي (تأكّد بـSQL: `account_uid`/`role`/`first_name`/`phone` مطابقة)
    - إشعار تجريبي أُدرج مباشرة بالقاعدة → فتح الجرس بالواجهة عرضه وصفّر العدّاد → تأكّد بـSQL أن `is_read` صار `true` فعلياً عبر الاستدعاء الحي (يثبت تقييد المنحة من الجلسة السابقة ما كسر الاستخدام الفعلي)
    - `owner.html` بتوكن مالك حقيقي (غير متعلّق بحساب) → عمل بدون أي اشتراط تسجيل دخول، بنفس السلوك القديم تماماً
    - بيانات الاختبار (البروفايل والإشعار) انحذفت بعدها من القاعدة. حسابات Auth الثلاثة التجريبية (`abdallatif.tiyah+audit-confirm-check-1/2@gmail.com`, `+livetest-signup-1@gmail.com`) بقيت بلوحة Auth → users — احذفها يدوياً من هناك لو حبيت قائمة نظيفة، ما قدرت أحذفها برمجياً (لا أداة Auth Admin API متاحة).
  - توثيق: أُضيف تحت «البنية» بـ`CLAUDE.md` وصف واقع التسجيل الجديد (بدون تأكيد، لماذا، الاسترجاع اليدوي، تحذير حذف مستخدم قبل التحقق من قاعدة الحذف)، وسطر جديد بجدول التشخيص عن «تسجيل يرجع ٢٠٠ ولا تصل رسالة».
- **الخطوة الجاية:** لا شيء معلّق على هالجزء. المتبقي من الجلسات السابقة: تجربة دخول فعلي بحساب طاقم حقيقي على `/admin`، وقراران مؤجّلان (ربط البروفايل القديم بالتوكن، ودخول OTP).

---

## آخر جلسة (سابقة — نفس اليوم)

- **التاريخ:** ٢٠٢٦-٠٩-٠٥ (متابعة) — تدقيق أمني على نظام الحسابات/الإشعارات القائم + إغلاقه
- **آخر commit:** (هالجلسة نفسها — بينعكس بالسجل الجاي)
- **شو صار:**
  - طُلب مني بناء نظام حسابات/إشعارات من الصفر بتصميم مختلف (`auth_user_id`/`account_claim_*`) — اكتشفت إنه نفس النظام **موجود أصلاً** بتصميم آخر (`account_uid`/`link_account_role`) من جلسة سابقة نفس اليوم، فأوقفت ورجعت للمستخدم بدل ما أبني نظاماً موازياً. القرار: تدقيق على الموجود لا بناء جديد.
  - **تدقيق أمني كامل** (مستخدمان وهميان بجلسة `authenticated` حقيقية عبر `set local role` + `request.jwt.claims`، لا فحص وصفي):
    - `link_account_role` **لا** تربط بمطابقة هاتف/اسم — تُنشئ صفاً جديداً دايماً أو ترجع صف المستخدم نفسه. لا ثغرة استيلاء.
    - `my_owner_dashboard`: رقم الباحث محجوب صح لما الحالة `new`، ظاهر لما تتغيّر — القاعدة ١٨ سليمة على السطح الجديد.
    - `notifications.self_update`: RLS يمنع النقل بين حسابات (`with check` سليم)، لكن المنحة كانت بلا قيد أعمدة → **مُصلَح** (تفصيل تحت).
    - كل الفحوص الثمانية (دوال إدارية، `v_admin_*`، `v_kpi_core`، `staff`، عزل بين حسابين، `has_function_privilege`) نجحت.
    - عدم انحدار: قراءات `anon` الخمس + دوال التسجيل العامة + cron `notify_expiring_soon` (آخر تشغيل نجح فعلياً) — كلها سليمة.
  - **فحص البريد (الأخطر بالتدقيق):** المشروع يستخدم خدمة Supabase المدمجة (`Enable custom SMTP` = **مطفأ**، تأكّد بلوحة `/auth/smtp`)، `Confirm email` = **مفعّل** (تأكّد بلوحة `/auth/providers`)، وسقف الإرسال الفعلي **٢ رسائل/ساعة** لكل المشروع (تأكّد بلوحة `/auth/rate-limits`). حسب توثيق Supabase الرسمي: الخدمة المدمجة **ترفض التسليم لأي عنوان مش عضو بفريق مشروع Supabase نفسه**. فريق منظمة Krioza عضو وحيد: `abdallatif.tiyah@gmail.com` (تأكّد بصفحة Team). **النتيجة: أي مستخدم حقيقي (غير عضو الفريق) لن تصله رسالة تأكيد إطلاقاً بالوضع الحالي — ليس مجرد تأخير، بل رفض تسليم من طبقة Auth نفسها.** لم أغيّر أي إعداد بريد — القرار معلّق لصاحب المشروع (ربط SMTP مخصّص شرط لأي تسجيل جماهيري).
  - **إصلاح منفّذ:** migration `20260905180028_restrict_notifications_update_columns` — `revoke update on notifications from authenticated` ثم `grant update (is_read) on notifications to authenticated` (طابقت العمود الفعلي اللي بيكتبه `account.html` بـ`grep`، مش `read_at` المفترض بالبرومبت). `service_role` احتفظ بمنحه الكاملة (تحقّق بـ`information_schema.role_column_grants`). اختبار عملي: تعليم `is_read=true` نجح، تعديل `title` انرفض (`permission denied for table notifications`)، محاولة تركيب نقل حساب مع `is_read` انرفضت أيضاً.
  - توثيق: صُحّحت القاعدة ١٦ بـ`CLAUDE.md` لتوضّح استثناء `p_actor` بدل التناقض معه، وأُضيف توضيح لمبدأ «الإشعارات عبر REST سليم عمداً»، وسطر جديد بجدول التشخيص عن فخ «RLS سليم لكن منحة أعمدة واسعة».
- **الخطوة الجاية:** قرار المستخدم بخصوص SMTP مخصّص (شرط لأي تسجيل جماهيري)، ثم استئناف تجربة `account.html`/`admin` الفعلية المؤجّلة من الجلسة السابقة.

---

## مهام مفتوحة — بترتيب الأولوية

- [ ] **مزوّد بريد مخصّص + إعادة تفعيل تأكيد البريد** — مرتبط بربط النطاق. الحل المؤقت الحالي (`Confirm email` مطفأ) يفتح التسجيل الفوري بدون بريد إطلاقاً، وهذا مقصود مؤقتاً فقط — أي مستخدم يُنشئ حساباً بأي بريد حتى لو غير مملوك له فعلياً، لأنه ما في تحقّق. عالج قبل أي إعلان علني عن الحسابات
- [ ] قرار مؤجّل: **ربط البروفايل القديم بالتوكن الموقّع** — حالياً المالك صاحب الإعلانات ينشئ حساباً فيحصل على بروفايل فارغ (`link_account_role` ما بتربط بمطابقة هاتف عمداً)، والربط يدوي بـ`admin_link_profile`. يُبنى حين يتكرر الربط اليدوي أكثر من مرة أسبوعياً
- [ ] قرار مؤجّل: **دخول برمز لمرة واحدة (OTP)** بجانب كلمة السر لا بدلاً منها — قابل للتراجع، عائد لصاحب المشروع
- [ ] حذف حسابات Auth التجريبية (`abdallatif.tiyah+audit-*`/`+livetest-*@gmail.com`) يدوياً من `/auth/users` إذا حبيت قائمة نظيفة — ما انحذفت برمجياً (لا أداة Auth Admin API متاحة بالجلسة)
- [ ] تجربة دخول فعلي بحساب طاقم حقيقي على `/admin` (البوابة نفسها تأكّدت، الدخول الفعلي لسا لأ)
- [ ] فجوة العرض ببيرزيت — مناطق معرّفة بالقاعدة، المخزون معدوم، والطلب غير مُتحقَّق منه ميدانياً بعد — كل بيانات الموقع تجريبية
- [ ] تعيين وتجهيز المندوب قبل ذروة الموسم
- [ ] تعبئة `name_en` يدوياً للمدن/المناطق الجديدة لما تنضاف (الموجودة معبّاة)
- [ ] استكمال التسجيل بوزارة الاقتصاد الوطني — شرط لفوترة المؤسسات
- [ ] قرار مستقبلي: إعادة تصميم بصرية كاملة لمركز التحكم (بقيت بتوكناتها القديمة عمداً هالجلسة)
- [ ] بعد بوابة القرار: ربط النطاق `sakan.ps` — بيد أبواللطيف، خارج نطاق Claude
- [ ] **`npx wrangler deploy`** لنشر عرض رسم النجاح الفعلي + كود الخصم النشط بنموذج «أضف غرفة» — الكود بالريبو من commit `7a53c9c`، غير منشور على الإنتاج بعد (تأكّد بـ`curl` مباشر على `sakan.abdallatif-tiyah.workers.dev`)

---

## سجل الجلسات (آخر ٥ — السادسة بتنحذف، ما في أرشفة)

| تاريخ | الجلسة | commit |
|---|---|---|
| ٢٠٢٦-٠٩-١٤ | تدقيق واستكمال نموذج الرسم حسب نوع السكن (مراحل ٣-٦: تحقّق `compute_fee_due`/`admin_fee_amount`، اختبار واجهة محلي، migrations لتثبيت `settings`/`SAKANNA50`) | (هالجلسة) |
| ٢٠٢٦-٠٩-١٤ | تغيير بادئة المرجع `MW-`←`SK-` + تصفير العدّاد، ثم إعادة تسمية «موضع» ← «سكنّا» (مرحلة ١ توثيق) | `abe2ed1`/`892cb1e` |
| ٢٠٢٦-٠٩-٠٦ | إعادة بناء `owner.html` كاملة + `owner_contact_status` + إصلاح باگين حيّين | `b671278` |
| ٢٠٢٦-٠٩-٠٥ | تشغيل التسجيل بدون تأكيد بريد بعد إطفاء Confirm email + اختبار حي منشور | `3c818de` |
| ٢٠٢٦-٠٩-٠٥ | تدقيق أمني على الحسابات/الإشعارات + تقييد منحة `notifications` + فحص بريد حاسم | `2d1b6a3` |
| ٢٠٢٦-٠٩-٠٥ | هوية بصرية + فصحى + حسابات/إشعارات اختيارية كاملة | `a3c1dea` |
